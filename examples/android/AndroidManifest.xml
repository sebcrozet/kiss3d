<?xml version="1.0" encoding="utf-8"?>
<!-- A pure NativeActivity app: no Java, no resources. android.app.lib_name
     names the cdylib that android-activity loads and whose android_main it
     calls. minSdk 26 is cpal's floor (AAudio); irrelevant here but kept so
     the harness matches what a real kiss3d/balaur game would declare. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.kiss3d.example">
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="35" />
    <application
        android:label="kiss3d"
        android:hasCode="false"
        android:extractNativeLibs="true">
        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <meta-data
                android:name="android.app.lib_name"
                android:value="kiss3d_android_example" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
